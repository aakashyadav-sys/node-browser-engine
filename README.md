# node-browser-engine
JSON Render Engine is a lightweight, from-scratch browser rendering pipeline built entirely in Node.js. It takes a JSON object representing a DOM-like tree and renders it into high-quality PNG images and vector-based, searchable PDF documents.
